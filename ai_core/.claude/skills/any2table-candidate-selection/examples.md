## Example 1
场景：
规则检索返回 2500 条 COVID 行证据，但用户只明确给了日期范围。

建议：
- 先按日期硬约束保留。
- 若辅助文档明显只讨论中国，则优先选择与中国主题一致的候选。
- 如果仍然无法确定，返回 `need_more_retrieval = false`，但在 `selection_notes` 中说明“当前仅完成粗筛”。

## Example 2
场景：
空气质量模板描述已明确“德州市，2025-11-25 09:00:00.0”。

建议：
- 保留同时满足城市与时间条件的站点行。
- 丢弃其他城市或其他时间的证据。
