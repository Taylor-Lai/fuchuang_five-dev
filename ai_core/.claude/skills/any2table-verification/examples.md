## Example 1
场景：
COVID 模板只要求部分范围，但系统写入了 2500 多条记录。

建议：
- 标记为 warning 或 review_needed。
- 提示“当前主要依赖日期粗筛，可能需要结合辅助文档主题进一步收窄”。

## Example 2
场景：
空气质量模板写入 79 条记录，并且都满足城市和时间限制。

建议：
- 若字段完整，输出 pass。
- reasoning_summary 里说明“结果与模板局部约束一致”。
